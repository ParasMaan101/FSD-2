# Experiment 5: React Lazy Loading with Suspense

## Key Concepts

### React.lazy()
- Used to dynamically import components
- The component loads only when it is rendered
- Must be used with Suspense boundary

### Suspense
- Wraps lazy components
- Displays fallback UI while the component is loading
- Improves user experience during component loading
